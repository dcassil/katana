---
id: mcp-tools-list-documents-search
level: task
title: "MCP tools: list_documents + search_documents"
short_code: "KAT-T-0090"
created_at: 2026-05-09T17:30:00+00:00
updated_at: 2026-05-18T21:06:01.094885+00:00
parent: KAT-I-0004
blocked_by: []
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
strategy_id: NULL
initiative_id: mcp-server-and-storage
---

## Parent Initiative

KAT-I-0004.

## Goal

Implement `list_documents` and `search_documents` MCP tools.

## Deliverables

- `src/mcp/tools/list_documents.ts`
- `src/mcp/tools/search_documents.ts`

## Architecture & Contracts

### `src/mcp/tools/list_documents.ts`

```ts
import type { StoragePort, ListFilter } from "../../storage/port.js";
import type { DocumentSummary } from "../../types/document.js";
import { TOOL_SCHEMAS } from "./schemas.js";

export const listDocumentsTool = {
  ...TOOL_SCHEMAS.list_documents,
  handler: async (input: ListFilter, ctx: { storage: StoragePort }): Promise<DocumentSummary[]> => {
    return ctx.storage.list({
      level: input.level,
      phase: input.phase,
      parent: input.parent,
      include_archived: input.include_archived ?? false,
      limit: input.limit ?? 100,
      offset: input.offset ?? 0,
    });
  },
};
```

### `src/mcp/tools/search_documents.ts`

```ts
import type { StoragePort, SearchFilter, SearchHit } from "../../storage/port.js";
import { TOOL_SCHEMAS } from "./schemas.js";

export const searchDocumentsTool = {
  ...TOOL_SCHEMAS.search_documents,
  handler: async (input: SearchFilter, ctx: { storage: StoragePort }): Promise<SearchHit[]> => {
    if (!input.query?.trim()) throw new Error("query must be non-empty");
    return ctx.storage.search({
      query: input.query,
      level: input.level,
      include_archived: input.include_archived ?? false,
      limit: input.limit ?? 25,
    });
  },
};
```

## Do / Don't

- **Do** apply default values exactly as specified in the JSON schema.
- **Do** rely on `StoragePort.search` to sanitize the FTS query.
- **Don't** return full document bodies in list/search results — summaries only.

## Acceptance

- list with `parent` filter returns exactly the children of that parent.
- search with a token present in one doc body returns that doc with non-empty `snippet`.

## Dependencies

KAT-T-0084, KAT-T-0087.