# Master Capability Table

| # | Layer | Capability | Decision | One-line rationale | Source survey | If Inherit: files | Consumers (initiatives) | Conflicts / gaps |
|---|-------|------------|----------|--------------------|---------------|-------------------|-------------------------|------------------|
| 1 | Persistence | Document storage layout | Inherit | Markdown + sqlite mirror directly solves katana's doc-driven workflow need; frontmatter provides rich metadata; hierarchical layout matches Product Doc → Epic → Story → Task tree. | survey-persistence.md#C1 | Directory builder, frontmatter parser, sqlite migrations, sync logic (unknown) | KAT-I-0006, KAT-I-0002, KAT-I-0001 | How do two-pass task variants (high-pass, low-pass) map to document storage? How does file_hash interact with agent-driven updates? Is archive directory mirroring MVP or post-MVP? |
| 2 | Persistence | Short codes | Inherit | Allocation and per-type uniqueness solve katana's document-identity need; UNIQUE constraint prevents collisions; prefix config supports multi-project instances; simple human-readable format. | survey-persistence.md#C2 | Format parser, per-type counter logic, uniqueness enforcement at create time (unknown) | KAT-I-0006, KAT-I-0001 | Concurrent agent document creation: does UNIQUE constraint block or error? How do two-pass task variants (HP, LP) map to short codes? Counter storage location (configuration vs. separate table)? |
| 3 | Persistence | Sync | Inherit | Read-only, idempotent sync is essential for doc-driven workflows + git + CI/CD; agents commit markdown; sync rebuilds index; hash-based detection sufficient. | survey-persistence.md#C3 | Directory scanner, YAML parser, file hash computation, upsert logic, FTS trigger integration (unknown) | KAT-I-0006 | File deletion handling: archived or deleted? Frontmatter parse error handling? Concurrent updates (agent + human): conflict detection? |
| 4 | Persistence | Archive | Defer | Housekeeping operation; does not affect core workflow; MVP uses phase="completed" to hide finished work; archive can be added post-MVP; well-isolated (CLI command, table column, directory move). | survey-persistence.md#C4 | N/A (deferred) | Post-MVP | Parent archive cascades to children? Archived documents unarchivable (terminal state)? Archived/ directory indexed for search/recovery or file-cleanup only? |
| 5 | Rules | Phase machine | Inherit | Metis phase machine is well-designed, database-backed, enforces forward-only constraints that map cleanly to katana's gating model; exit-criteria gating is native and directly supports katana's gate engine vision. | survey-rules.md#C1 | Rust modules handling phase transitions and validation (unknown; metis is binary, not exposed library) | KAT-I-0001 | Phase machine phases are hard-coded in metis binary; katana must fork or wrap. Exit-criteria gating: unclear if metis enforces before transitions or advisory only. Custom phases beyond 5–6 standard: supported in config or source modification required? Phase tags auto-update on transition or manual sync required? |
| 6 | Rules | Lint | Inherit | Metis lint is robust, rules-based, with severity levels and custom rule support; directly supports katana's gate engine by validating document state before phase changes; extending rules for new doc types is more efficient than building from scratch. | survey-rules.md#C2 | Rust modules implementing rule parsing, severity classification, CLI output formatting (unknown) | KAT-I-0006, KAT-I-0002 | Lint rule evaluation order and composition: fixed order or overridable? Can custom rules reference custom frontmatter fields? How are `[lint.phase_gates]` used? Document-to-document cross-validation supported? |
| 7 | Rules | Validate | Depend | Metis validate provides minimal format checking needed to ensure documents are machine-readable before further processing; rather than fork, katana should depend on metis validate CLI for document acceptance. | survey-rules.md#C3 | N/A (Depend decision) | KAT-I-0006, KAT-I-0002 | Is validate exposed via MCP, or only CLI? Runs sync automatically or only on explicit call? Can validate accept custom frontmatter fields without forking metis? If katana doc uses new type not in metis, will validate reject it? |
| 8 | Query | Search | Inherit | FTS5 implementation with Porter stemmer is production-ready and well-tested in metis; inheriting avoids reimplementing full-text search from scratch; tokenizer handles English morphology and Unicode text. | survey-query.md#C1 | FTS5 virtual table definition, documents_ai/au/ad triggers, query sanitization logic, repository methods search_documents/unarchived, limit/format handling | KAT-I-0001, KAT-I-0006 | FTS5 rebuild cost if corpus grows large (performance testing needed). Query sanitization may be over-protective for agents. No non-English stemming; multilingual support unknown. |
| 9 | Query | List | Inherit | Filtering and sorting logic is minimal and stable; multi-format output (JSON, compact, table) already supports agent integration; no custom DSL needed; database indexes ensure O(log n) lookups. | survey-query.md#C2 | Repository methods find_by_type/phase/type_and_phase/all, OutputFormat enum and display logic, database indexes on type/phase columns, filter pipeline logic | KAT-I-0001, KAT-I-0006 | No full-text search integration with filtering (separate commands). Phase filtering assumes standardized phase names; divergence breaks queries. Performance of find_all for 10k+ documents may require pagination. |
| 10 | Query | Status | Depend | Priority tier logic is useful for humans; relative timestamps ("2 minutes ago") are not machine-parseable for agent loops; katana would need true ISO 8601 timestamps; depending on metis status avoids reimplementing phase-to-priority mapping. | survey-query.md#C3 | N/A (Depend decision) | KAT-I-0001 | Blocker for agent loops: relative timestamps are lossy; agent needs true updated_at (ISO 8601). Workaround: query database directly or request metis add --format=agent-json. `blocked_by` field not populated in status output. No completed/archived count summary. No metrics (count by phase). |
| 11 | Agent-Integration | Document templates | Inherit | Template engine + fallback chain is exactly what katana needs to add new doc types without forking metis; built-in defaults + project-level override allows custom templates while keeping metis sources clean; Tera substitution handles gate metadata. | survey-agent-integration.md#C1 | TemplateLoader service, template fallback logic, DocumentType enum, domain/documents/{type}/ files, template embedding (include_str! macro) | KAT-I-0002, KAT-I-0001 | Can template engine add new doc types without forking metis? YES but forking DocumentType enum required. Frontmatter schema validation: enum-based or config-extensible? Template variables extensible without code changes? |
| 12 | Agent-Integration | Configuration presets | Defer | Presets are fixed, read-only config; cannot be customized without forking metis; hardcoded in match statement; katana may require finer-grained per-type control; deferred until katana's final doc-type hierarchy determined. | survey-agent-integration.md#C2 | N/A (deferred) | Post-MVP | Can presets be extended without forking? No. Is strategies_enabled field vestigial? Yes. Will katana's gate model fit within Preset/Initiatives toggle, or need finer control per type? |
| 13 | Agent-Integration | Agent-rules generation | Skip | Metis does not provide CLAUDE.md injection, Cursor rules generation, or agent-rules platform; MCP tools list includes only document CRUD, not agent-rules generation; metis is passive—documents created/transitioned externally; no rule-generation CLI/MCP. | survey-agent-integration.md#C3 | N/A (Skip decision) | Post-MVP (new agent-platform initiative) | How will agents invoke katana workflows? Via MCP or native APIs? Should agent rules be version-controlled or regenerated on init? Cursor .mdc rule syntax—standardized or version-dependent? |
| 14 | Agent-Integration | Code-index | Defer | Code-index is self-contained feature (separate crate, skill, MCP tool, CLI); MVP should focus on gate/task workflow first; structural indexing (tree-sitter) is reusable; semantic summarization can be plugged in post-MVP. | survey-agent-integration.md#C4 | N/A (deferred) | Post-MVP | Incremental indexing tracks content changes only or directory structure too? Code-index-summarizer context-window handling for large modules? Supported languages: Rust, Python, TS/JS, Go. Extensions needed (C#, Java, etc.)? |
| 15 | MCP | initialize_project | Inherit | Core operation; generic enough for katana to mirror cleanly; not tightly coupled to metis-specific doc types beyond the concept of a "project" root; katana will initialize with own document types and phase machine. | survey-mcp.md#initialize_project | N/A (generic operation signature) | KAT-I-0006 | Verify katana's document-type schema differs enough from metis to justify separate implementation. Confirm prefix length and character constraints match katana naming. |
| 16 | MCP | create_document | Inherit | Core CRUD operation; generic contract (accept type, parent, title, metadata) is adaptable; moderately coupled to document types but can be extended per katana hierarchy; backlog-category support is orthogonal. | survey-mcp.md#create_document | N/A (generic operation signature) | KAT-I-0006, KAT-I-0001 | Katana's document types must be defined before implementing; document-type coupling is main risk. Confirm katana supports backlog-style items or strict hierarchy only. |
| 17 | MCP | read_document | Inherit | Essential read-only operation; completely generic; no coupling to metis doc types beyond "document"; storage format may differ but read contract is universal. | survey-mcp.md#read_document | N/A (generic operation signature) | KAT-I-0006, KAT-I-0001 | Confirm katana document storage format (YAML + markdown) matches metis or requires adaptation. Verify short-code scheme identical in katana. |
| 18 | MCP | edit_document | Inherit | Core mutation operation; completely generic; search-and-replace is storage-agnostic; no coupling to doc types or phases. | survey-mcp.md#edit_document | N/A (generic operation signature) | KAT-I-0006, KAT-I-0001 | Verify search-and-replace semantics (whitespace, multiline) match katana's editing needs. Consider whether YAML frontmatter editable via MCP or only metadata operations. |
| 19 | MCP | list_documents | Inherit | Essential introspection operation; completely generic; boolean toggle for archive is simple; no coupling to doc types or phases. | survey-mcp.md#list_documents | N/A (generic operation signature) | KAT-I-0006, KAT-I-0001 | Verify output schema sufficient for katana's UI and integrations. Confirm whether filtering by type/phase should be MVP or deferred. |
| 20 | MCP | search_documents | Inherit | Essential search capability; completely generic; FTS is storage-agnostic; type filtering decoupled from type-specific logic. | survey-mcp.md#search_documents | N/A (generic operation signature) | KAT-I-0001 | Verify ripgrep and FTS5 indexing strategy appropriate for katana. Confirm whether scoring/ranking should be customized. |
| 21 | MCP | transition_phase | Inherit | Core workflow operation; tightly coupled to phase machine concept but operation itself (short code → state machine) is generic; phase definitions differ per doc type but MCP contract is phase-agnostic. | survey-mcp.md#transition_phase | N/A (generic operation signature) | KAT-I-0006, KAT-I-0001 | Katana's phase definitions must be finalized before MCP design; major dependency. Verify whether exit-criteria checking should be mirrored or deferred to lint-style validation. |
| 22 | MCP | archive_document | Inherit | Core document lifecycle operation; generic concept (mark inactive, move to archive); assumes hierarchical structure (cascade archival) which katana likely supports. | survey-mcp.md#archive_document | N/A (generic operation signature) | KAT-I-0006 | Confirm katana supports parent-child relationships and cascade archival. Verify whether archived docs queryable or completely hidden. |
| 23 | MCP | reassign_parent | Defer | Specialized operation tied to metis's task/initiative/backlog hierarchy and backlog-category enum; useful for workflow but not essential to MVP; defer until katana's backlog and task-reassignment semantics finalized. | survey-mcp.md#reassign_parent | N/A (deferred) | Post-MVP | Backlog categories (bug/feature/tech-debt) are metis-specific; katana may use different taxonomy. Verify whether katana supports moving items out of initiatives. |
| 24 | MCP | lint_document | Defer | Validation/quality tooling not essential to MVP; lint rules highly coupled to doc-type definitions, exit criteria, phase transitions; defer until katana's validation framework designed. | survey-mcp.md#lint_document | N/A (deferred) | Post-MVP | Lint rules must be defined per katana doc type. Decide whether linting reactive (on-demand) or embedded in phase-transition gates. |
| 25 | MCP | lint_workspace | Defer | Batch validation operation not essential to MVP; same coupling issues as lint_document; performance considerations at scale. | survey-mcp.md#lint_workspace | N/A (deferred) | Post-MVP | Same as lint_document, but at scale. Performance unknown for large workspaces. Decide whether MCP tool or internal CLI only. |
| 26 | MCP | arch_lint | Skip | Specialized to code architecture validation, not document structure; relies on metis's architecture-pattern registry; tangential to katana's document-management mission; revisit if katana adopts guard-rails. | survey-mcp.md#arch_lint | N/A (Skip decision) | N/A | If katana integrates code auditing/architecture scanning, may become relevant post-MVP. Unclear whether katana scope includes architecture enforcement. |
| 27 | MCP | list_patterns | Inherit | Read-only introspection of available patterns; completely generic; useful for UIs and pattern selection; contract universally applicable if katana adopts pattern system. | survey-mcp.md#list_patterns | N/A (generic operation signature) | Post-MVP | Confirm katana's pattern system shares metis's concept of "layers," "built-in," "project-local." Verify pattern metadata schema sufficient for katana. |
| 28 | Katana Addition | Two-pass task model | MVP | Vision: "high-pass task run by stronger model…followed by low-pass task…Each task type is a distinct document so the workflow knows which model to dispatch and what to validate." Adds two new document types, requires distinct phase sets and templates. | survey-katana-additions.md#A1 | Phase machine extension (new doc types), sqlite schema (new rows in document_types), frontmatter (new task_type field), templates (scaffold vs. fill-in) | KAT-I-0006, KAT-I-0002 | Do Task[high-pass] and Task[low-pass] have identical phase sets or different ones? Parent-child relationship or independent? Validation/lint rules specific to each? |
| 29 | Katana Addition | Pluggable agent-platform adapter | MVP | Vision: "pluggable agent-platform adapter (Claude slash commands and skills, Cursor rules, OpenAI/Codex equivalents, etc.) so the same workflow runs anywhere." Same katana workflow on Claude, Cursor, Codex. | survey-katana-additions.md#A2 | Abstract adapter interface with concrete implementations per platform; MCP becomes canonical interface; platform-specific wrappers invoke MCP | Post-MVP | What is adapter contract? Just "expose these MCP tools via agent's native mechanism"? How are platform-specific extensions (e.g., Cursor AI edits) surfaced? Discovery/registration mechanism for new platforms? |
| 30 | Katana Addition | Pluggable kanban-board adapter | Post-MVP | Vision: "pluggable kanban board (internal-default, swappable to Jira/Linear/etc.)." MVP does not include Jira/Linear integration; interface must accommodate it. | survey-katana-additions.md#A3 | New subsystem: board adapter interface, default internal board, MCP surface (list, create, update status); no impact on metis layers | Post-MVP | Minimal set of board operations? Create/list/update/delete? Document archive/decompose/transition → board state mapping? One-way or bidirectional sync? Orphaned work items handling? |
| 31 | Katana Addition | Pluggable storage backend | Post-MVP | Vision: "pluggable storage backend (sqlite+md by default, other backends possible)." MVP constraint: "sqlite+md by default" implicit; alternatives deferred. | survey-katana-additions.md#A4 | Abstract backend interface; sqlite+md as concrete adapter; sync, archive, schema migration become backend-specific; phase machine + lint remain backend-agnostic | Post-MVP | Backend contract (store, retrieve, FTS search, allocate short code, list, archive, sync)? Short-code uniqueness enforcement across backends? Interface support for full metis query set (search, list, filters, status)? Schema migration story? |
| 32 | Katana Addition | Gate engine as first-class composable config | MVP | Vision: "Gates that block downstream decomposition until upstream docs meet completeness criteria, and block phase progression until exit criteria are satisfied." Success criterion: "team can…produce docs without writing custom code." Gate logic in markdown+config, not hardcoded. | survey-katana-additions.md#A5 | Phase machine refactored to invoke gate-check hooks; new gate-engine subsystem; rule evaluation from config; pre-decomposition, exit-criteria, parent-readiness gate types | KAT-I-0001 | Gate rule syntax? YAML/markdown/DSL? Gates can reference other documents? Sync'd to sqlite or stored separately? Agent interaction on gate failure—get suggestions? |
| 33 | Katana Addition | Work-eval-gate-loop runtime per task | MVP | Vision: "workflow loop (work → eval → gate → loop → done) that drives a doc through its phases with the right model for each step." Success criterion: "two-pass execution measurably improves weak-model output quality vs. one-pass." | survey-katana-additions.md#A6 | Phase machine becomes loop input; loop invokes MCP tools for document mutation; agents call loop (skill or MCP or service); loop calls back to model/validator | KAT-I-0001 | Loop as skill/MCP tool/service? Eval step: model call or linter or both? Loop parameters (model tier, temp, tokens) per task? Pausable/resumable or atomic per phase? Diagnostics/logs emitted? |
| 34 | Katana Addition | New document hierarchy | MVP | Vision: "document hierarchy (Product Doc → Epic → User Story → Task[high-pass] → Task[low-pass]) with template files and required-field validation." Extended from metis's Vision → Strategy → Initiative → Task. | survey-katana-additions.md#A7 | SQLite document_type enum (7 values); phase sets per type; parent constraints (Product Doc → Epic → User Story → Task); templates for each; lint/decompose type-specific | KAT-I-0006, KAT-I-0002, KAT-I-0001 | Task[UI] always optional or required for certain initiatives? User Story: can have both high-pass + low-pass tasks, or sequential? 1:1 mapping to metis hierarchy or migration needed? Terminal document types (cannot decompose)? |
