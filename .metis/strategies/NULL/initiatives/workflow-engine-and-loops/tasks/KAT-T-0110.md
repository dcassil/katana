---
id: gate-adapter-bridge-kat-i-0002
level: task
title: "Gate adapter — bridge KAT-I-0002 gate runner to the loop"
short_code: "KAT-T-0110"
created_at: 2026-05-09T18:00:00+00:00
updated_at: 2026-05-11T13:35:04.767802+00:00
parent: KAT-I-0003
blocked_by: [KAT-T-0101]
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
strategy_id: NULL
initiative_id: workflow-engine-and-loops
---

## Parent Initiative
KAT-I-0003 — Workflow Engine and Task Loops.

## Goal
Wrap the gate engine (KAT-I-0002 — `src/gates/*`) in a narrow adapter the loop runtime calls. Keep the loop ignorant of `Gate`, `GateContext`, and `GateConfig`.

## Deliverables
- New file: `/Users/danielcassil/Code/katana/src/workflow/gate-adapter.ts`.

## Architecture & Contracts

### Inline-restated dependencies (from KAT-I-0002)

These are the exact shapes from `src/gates/types.ts`, `src/gates/runner.ts`, `src/gates/context.ts`, and `src/gates/config-schema.ts`. The adapter consumes them but the loop never sees them.

```ts
// src/gates/types.ts
export interface KatanaDocument {
  path: string;
  raw: string;
  frontmatter: { /* DocumentFrontmatter, partial */ short_code?: string; level?: string; phase?: string; [k: string]: unknown };
  body: string;
  sections: { heading: string; depth: number; body: string }[];
}
export type GateSeverity = "error" | "warning";
export interface GateReason { code: string; message: string; severity: GateSeverity; pointer?: string }
export interface GateResult { gate: string; ok: boolean; reasons: GateReason[] }
export interface GateContext { lookup(sc: string): KatanaDocument | null; all(): KatanaDocument[] }
export interface Gate { readonly name: string; evaluate(doc: KatanaDocument, ctx: GateContext): GateResult }

// src/gates/runner.ts
export interface GateRunReport {
  document: { path: string; short_code: string | undefined };
  results: GateResult[];
  ok: boolean;
}
export function runGates(gates: ReadonlyArray<Gate>, doc: KatanaDocument, ctx: GateContext): GateRunReport;

// src/gates/context.ts
export function createContext(documents: ReadonlyArray<KatanaDocument>): GateContext;

// src/gates/config-schema.ts
export interface GateTriggerRule {
  trigger:
    | { kind: "phase-transition"; level: string; to: string }
    | { kind: "decomposition"; level: string }
    | { kind: "create"; level: string }
    | { kind: "edit"; level: string };
  gates: string[];
}
export interface GateConfig { version: 1; disabled: string[]; rules: GateTriggerRule[] }
```

### File contents

```ts
/**
 * GateAdapter: the only surface the loop runtime uses to call gates.
 * Translates a katana Document (storage shape) to a KatanaDocument
 * (gate shape), picks the gate set from GateConfig keyed on the
 * "phase-transition: <level> → <next-phase>" trigger, and runs them.
 *
 * Returns a flat failure list for LoopIteration.gateFailures.
 */
import type { Document } from "../types/document.js";
import type { Gate, KatanaDocument, GateContext } from "../gates/types.js";
import { runGates, type GateRunReport } from "../gates/runner.js";
import { createContext } from "../gates/context.js";
import type { GateConfig } from "../gates/config-schema.js";
import { nextPhase } from "./phase-machine.js";
import type { GateFailureSummary } from "./types.js";

export interface GateAdapterReport {
  ok: boolean;
  failures: GateFailureSummary[];
  /** Full underlying report, for diagnostic logging. */
  raw: GateRunReport;
}

export interface GateAdapter {
  evaluate(doc: Document): Promise<GateAdapterReport>;
}

/** Dependencies the adapter needs from the host: gate registry + workspace docs. */
export interface GateAdapterDeps {
  /** Map of gate-name → Gate impl (built-ins from src/gates/builtin/* + project gates). */
  registry: ReadonlyMap<string, Gate>;
  /** Effective gate config (already merged with defaults). */
  config: GateConfig;
  /** Snapshot of all non-archived docs for ctx.lookup / ctx.all. */
  workspace: () => ReadonlyArray<Document>;
}

export function createGateAdapter(deps: GateAdapterDeps): GateAdapter {
  return {
    async evaluate(doc: Document): Promise<GateAdapterReport> {
      const gates = selectGates(doc, deps.config, deps.registry);
      const all = deps.workspace().map(toGateDoc);
      const ctx: GateContext = createContext(all);
      const report = runGates(gates, toGateDoc(doc), ctx);
      const failures: GateFailureSummary[] = report.results.flatMap((r) =>
        r.reasons
          .filter((reason) => reason.severity === "error")
          .map((reason) => ({
            gate: r.gate,
            code: reason.code,
            message: reason.message,
            pointer: reason.pointer,
          })),
      );
      return { ok: report.ok, failures, raw: report };
    },
  };
}

/** Choose gates by the "phase-transition" trigger keyed on (level, next phase). */
function selectGates(
  doc: Document,
  config: GateConfig,
  registry: ReadonlyMap<string, Gate>,
): ReadonlyArray<Gate> {
  const level = doc.frontmatter.level;
  const to = nextPhase(level, doc.frontmatter.phase);
  if (!to) return []; // already terminal — no transition gate to run.
  const rule = config.rules.find(
    (r) => r.trigger.kind === "phase-transition" && r.trigger.level === level && r.trigger.to === to,
  );
  if (!rule) return [];
  const disabled = new Set(config.disabled);
  return rule.gates
    .filter((name) => !disabled.has(name))
    .map((name) => registry.get(name))
    .filter((g): g is Gate => g !== undefined);
}

/** Storage Document → gate KatanaDocument. */
function toGateDoc(doc: Document): KatanaDocument {
  return {
    path: doc.filepath,
    raw: "",                 // not used by current built-in gates
    frontmatter: doc.frontmatter as unknown as KatanaDocument["frontmatter"],
    body: doc.body,
    sections: parseSectionsLite(doc.body),
  };
}

/** Local copy of src/gates/loader.ts#parseSections to avoid pulling fs. */
function parseSectionsLite(body: string): KatanaDocument["sections"] {
  const HEADING = /^(#{2,6})\s+(.+?)\s*$/;
  const out: KatanaDocument["sections"] = [];
  let cur: { heading: string; depth: number; lines: string[] } | null = null;
  for (const line of body.split(/\r?\n/)) {
    const m = HEADING.exec(line);
    if (m) {
      if (cur) out.push({ heading: cur.heading, depth: cur.depth, body: cur.lines.join("\n").trim() });
      cur = { heading: m[2]!, depth: m[1]!.length, lines: [] };
    } else if (cur) {
      cur.lines.push(line);
    }
  }
  if (cur) out.push({ heading: cur.heading, depth: cur.depth, body: cur.lines.join("\n").trim() });
  return out;
}
```

## Do / Don't
- DO key gate selection on `(level, nextPhase)` — the gates that must pass to *advance* are the gates of the next transition.
- DO drop only `error`-severity reasons into `failures`; warnings are advisory.
- DO honor `config.disabled`.
- DON'T import `loadDocument` from `src/gates/loader.ts` — that does fs I/O. Reproduce only the parser inline.
- DON'T call `runGates` outside `createGateAdapter` — the loop must go through the adapter.
- DON'T mutate `Document` or `KatanaDocument`.

## Acceptance
- [ ] File exists at the exact path.
- [ ] Exports: `GateAdapter`, `GateAdapterReport`, `GateAdapterDeps`, `createGateAdapter`.
- [ ] When `nextPhase` returns null, `evaluate` returns `{ ok: true, failures: [] }` (no gates to run, nothing to fail).
- [ ] Disabled gate names in `config.disabled` never appear in the executed list.
- [ ] `tsc --noEmit` passes.

## Dependencies
- KAT-T-0101 (workflow types), KAT-T-0102 (`nextPhase`), KAT-I-0002 (`src/gates/*`).