---
id: two-pass-handoff-enforcement-src
level: task
title: "Two-pass handoff enforcement (src/workflow/handoff.ts)"
short_code: "KAT-T-0104"
created_at: 2026-05-09T18:00:00+00:00
updated_at: 2026-05-11T13:35:00.458792+00:00
parent: KAT-I-0003
blocked_by: [KAT-T-0101]
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
strategy_id: NULL
initiative_id: workflow-engine-and-loops
---

## Parent Initiative
KAT-I-0003 — Workflow Engine and Task Loops.

## Goal
Engine-side enforcement of the two-pass handoff: a `task-low-pass` cannot start (transition `todo → active`) until its `scaffold_task` has reached phase `completed`.

## Deliverables
- New file: `/Users/danielcassil/Code/katana/src/workflow/handoff.ts`.

## Architecture & Contracts

### Inline-restated dependencies
The engine resolves documents by short code through a small port that any storage backend can satisfy. We define it inline so this module has no compile dependency on the in-progress `StoragePort` (KAT-I-0004):

```ts
/** Minimal projection the handoff module needs. The full Document type
 *  in src/types/document.ts is a strict superset; pass it directly. */
export interface HandoffDocView {
  short_code: string;
  level: "product-doc" | "epic" | "user-story"
       | "task-high-pass" | "task-low-pass" | "task-ui";
  phase: string;
  scaffold_task?: string;
}

export interface DocLookup {
  /** Return the doc with this short code, or null if unknown. */
  get(short_code: string): HandoffDocView | null;
}
```

### File contents

```ts
/**
 * Two-pass handoff enforcement. Pure (no I/O); depends only on a
 * DocLookup the caller supplies.
 *
 * Rule: a task-low-pass MUST have `scaffold_task` set, that scaffold
 * MUST resolve to a task-high-pass document (per
 * docs/schema/frontmatter.md), and that task-high-pass MUST be in
 * phase "completed" before the low-pass may transition todo → active.
 *
 * The loop runtime (src/workflow/loop.ts) calls `assertCanStart` BEFORE
 * dispatching any model work. The MCP transition_phase handler (storage
 * layer) calls it as a guard for the same transition.
 */
import type { ShortCode } from "../types/document.js";
import type { TransitionRejection } from "./types.js";

export interface HandoffDocView {
  short_code: string;
  level:
    | "product-doc"
    | "epic"
    | "user-story"
    | "task-high-pass"
    | "task-low-pass"
    | "task-ui";
  phase: string;
  scaffold_task?: string;
}

export interface DocLookup {
  get(short_code: string): HandoffDocView | null;
}

/**
 * Returns null if the low-pass may start; otherwise a structured rejection.
 * No-ops (returns null) for any non-low-pass document — the rule only
 * applies to task-low-pass.
 */
export function assertCanStart(
  doc: HandoffDocView,
  lookup: DocLookup,
): TransitionRejection | null {
  if (doc.level !== "task-low-pass") return null;

  const scaffoldCode = doc.scaffold_task;
  if (!scaffoldCode) {
    return {
      kind: "scaffold-incomplete",
      lowPass: doc.short_code as ShortCode,
      scaffold: "" as ShortCode,
      scaffoldPhase: "missing",
    };
  }

  const scaffold = lookup.get(scaffoldCode);
  if (!scaffold) {
    return {
      kind: "scaffold-incomplete",
      lowPass: doc.short_code as ShortCode,
      scaffold: scaffoldCode as ShortCode,
      scaffoldPhase: "missing",
    };
  }

  if (scaffold.level !== "task-high-pass") {
    // Per schema, scaffold_task MUST point to a task-high-pass.
    return {
      kind: "scaffold-incomplete",
      lowPass: doc.short_code as ShortCode,
      scaffold: scaffoldCode as ShortCode,
      scaffoldPhase: "missing",
    };
  }

  if (scaffold.phase !== "completed") {
    return {
      kind: "scaffold-incomplete",
      lowPass: doc.short_code as ShortCode,
      scaffold: scaffoldCode as ShortCode,
      scaffoldPhase: scaffold.phase,
    };
  }

  return null;
}

/**
 * Convenience: format a scaffold-incomplete rejection as a one-line
 * human message for logs / agent surfaces.
 */
export function explainHandoffRejection(r: TransitionRejection): string {
  if (r.kind !== "scaffold-incomplete") return `(non-handoff rejection: ${r.kind})`;
  if (r.scaffoldPhase === "missing") {
    return `Low-pass ${r.lowPass} cannot start: scaffold ${r.scaffold || "<unset>"} not found.`;
  }
  return `Low-pass ${r.lowPass} cannot start: scaffold ${r.scaffold} is in phase "${r.scaffoldPhase}", needs "completed".`;
}
```

## Do / Don't
- DO keep this module pure — the only dependency is the injected `DocLookup`.
- DO short-circuit non-low-pass docs to `null` so callers can call it unconditionally.
- DON'T call the gate engine from here — gate failures are a separate axis (KAT-T-0110).
- DON'T mutate documents.
- DON'T fall back to "any task-* counts as a scaffold" — schema requires task-high-pass.
- DON'T cache lookups internally; the caller owns lifetime.

## Acceptance
- [ ] File exists at the exact path with both exported functions.
- [ ] `tsc --noEmit` passes.
- [ ] Returns null for product-doc/epic/user-story/task-high-pass/task-ui regardless of scaffold field.
- [ ] All four scaffold-incomplete branches emit the right `scaffoldPhase` value (`"missing"` for unset, unknown, wrong-level; literal phase string when high-pass is non-completed).
- [ ] Unit tests (KAT-T-0105) pass.

## Dependencies
- KAT-T-0101 (workflow types).