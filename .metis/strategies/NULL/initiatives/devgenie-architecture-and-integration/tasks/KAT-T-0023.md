---
id: add-katana-entry-to-dev-genie
level: task
title: "Add katana entry to dev-genie orchestration registry"
short_code: "KAT-T-0023"
created_at: 2026-05-09T16:30:00+00:00
updated_at: 2026-05-18T21:44:58.003064+00:00
parent: KAT-I-0007
blocked_by: []
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
strategy_id: NULL
initiative_id: devgenie-architecture-and-integrati
---

## Parent Initiative

[KAT-I-0007 dev-genie Architecture Pattern and Integration](../initiative.md)

## Goal

Append a `katana` entry as the third numbered item in the "Sub-plugin registry" of dev-genie's orchestration skill, following the exact bullet shape used by the existing `guardrails` and `audit` entries. Order rationale: katana runs after audit because it depends on neither and is additive; it is also optional in the greenfield walk.

## Deliverables

- Modify: `/Users/danielcassil/Code/gaurd-rails-boilerplate/dev-genie/skills/orchestration/SKILL.md`

## Architecture & Contracts

Insert a new section directly under the existing `### 2. audit` block, before the `## Adding a new sub-plugin` section. Use this exact shape (matches existing entries):

```markdown
### 3. katana

- **Purpose**: agent-driven kanban workflow that decomposes work product-doc → epic → story → task[high-pass] → task[low-pass]; ships as Claude Code plugin + MCP server + `.katana/` workspace.
- **Install check**: a katana plugin is reachable. Practical signals: `${CLAUDE_PLUGIN_ROOT}/../katana/` exists, OR a `katana/` directory exists at the workspace root, OR the `/katana-init` slash command is registered. If `.katana/` already exists in the target repo, treat katana as already-installed and confirm with the user before re-running.
- **Setup command**: `/katana-init`.
- **Post-setup verification**: confirm `.katana/config.toml` and `.katana/vision.md` exist in the target repo, and that the katana MCP server is registered (e.g. visible in `claude mcp list`).
- **Notes**: katana is standalone-first. The greenfield walk MUST ask the user whether to install katana (it is optional; not every project wants kanban-shaped doc decomposition). Default to "no" unless project-detection signals an agent-workflow project.
```

Final-state checklist additions (append to the existing checklist near the bottom of the file):

```markdown
- [ ] If katana was offered and accepted: `.katana/config.toml` and `.katana/vision.md` exist, and `claude mcp list` shows the katana MCP server.
- [ ] If katana was declined: the orchestration log records the decline and recommends `/katana-init` as a follow-up command.
```

## Do / Don't

- Don't modify guardrails or audit sub-plugins; this initiative only touches dev-genie itself and adds the new pattern entry.
- Don't introduce a separate config file or registry format — append to the markdown registry, per the skill's own "Adding a new sub-plugin" instructions.
- Don't reorder existing entries. katana goes after audit.
- Don't hard-code katana paths anywhere except inside this registry entry; runtime paths come from the registry.
- Don't add detection-for-katana logic in this task — that's KAT-T-0024.
- Don't claim the install command exists yet; this task only registers it.

## Acceptance

- [ ] A `### 3. katana` section exists in `orchestration/SKILL.md`.
- [ ] All four required fields (Purpose, Install check, Setup command, Post-setup verification) are present.
- [ ] At least one new line under the Final-state checklist references katana.
- [ ] The `## Adding a new sub-plugin` section is unchanged.
- [ ] No file outside `dev-genie/skills/orchestration/SKILL.md` is modified.

## Dependencies

KAT-T-0020