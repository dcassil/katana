---
id: adr-does-katana-need-a-new-dev
level: task
title: "ADR: does katana need a new dev-genie arch pattern"
short_code: "KAT-T-0021"
created_at: 2026-05-09T16:30:00+00:00
updated_at: 2026-05-18T21:38:26.363101+00:00
parent: KAT-I-0007
blocked_by: []
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
strategy_id: NULL
initiative_id: devgenie-architecture-and-integrati
---

## Parent Initiative

[KAT-I-0007 dev-genie Architecture Pattern and Integration](../initiative.md)

## Goal

Decide and record whether katana fits an existing `guardrails:arch-*` pattern or requires a new architecture pattern entry called `agent-workflow-mcp-workspace`. Produce a short ADR-style decision document in the initiative directory. Decision is informed by the contract reference (KAT-T-0020) and a side-by-side compare of katana's footprint vs. the existing arch patterns.

## Deliverables

- Create: `/Users/danielcassil/Code/katana/.metis/strategies/NULL/initiatives/devgenie-architecture-and-integration/adr-arch-pattern-decision.md`

## Architecture & Contracts

The ADR must use this skeleton:

```markdown
# ADR: katana arch-pattern fit in dev-genie

## Status
Decided | Draft

## Context
Katana = Claude Code plugin + MCP server + doc workspace. Existing patterns
(`arch-next-vercel`, `arch-node-api`, `arch-supabase-api`, `arch-supabase-node-rag`)
all ship `eslint.config.mjs` + `tsconfig.json` for an application repo.

## Options
1. Reuse `arch-node-api` — katana's runtime is a Node MCP server.
2. Add new pattern `agent-workflow-mcp-workspace`.
3. Skip the pattern catalog entirely — register katana only in the orchestration registry.

## Decision
<one of the three, with one-paragraph rationale>

## Consequences
- Files added/modified
- Follow-up tasks (reference KAT-T-XXXX)
```

Reference the existing pattern shapes by absolute path:
- `/Users/danielcassil/Code/gaurd-rails-boilerplate/guardrails/skills/arch-node-api/SKILL.md`
- `/Users/danielcassil/Code/gaurd-rails-boilerplate/guardrails/skills/guard-rails-catalog/SKILL.md`

Default recommendation (executor may override only with explicit rationale): **Option 2** — add a new pattern `agent-workflow-mcp-workspace`. Rationale: existing patterns ship lint+ts configs for an app repo; katana's footprint is a plugin + MCP + workspace, not lint rules.

## Do / Don't

- Do pick exactly one option and state it in the Status/Decision sections.
- Do reference KAT-T-0020's contract-reference.md.
- Don't implement the pattern in this task — only decide. Implementation is a separate task.
- Don't modify any file under `/Users/danielcassil/Code/gaurd-rails-boilerplate/`.
- Don't relitigate katana's product scope — that lives in the vision and the initiative.

## Acceptance

- [ ] ADR file exists at the path above.
- [ ] Status field is `Decided`.
- [ ] Exactly one of the three options is selected in the Decision section.
- [ ] Consequences section names at least one follow-up task short code.

## Dependencies

KAT-T-0020