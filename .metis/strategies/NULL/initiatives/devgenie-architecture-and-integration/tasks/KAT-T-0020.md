---
id: audit-dev-genie-sub-plugin
level: task
title: "Audit dev-genie sub-plugin contract and write contract reference"
short_code: "KAT-T-0020"
created_at: 2026-05-09T16:30:00+00:00
updated_at: 2026-05-18T21:38:25.338558+00:00
parent: KAT-I-0007
blocked_by: []
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
strategy_id: NULL
initiative_id: devgenie-architecture-and-integrati
---

## Parent Initiative

[KAT-I-0007 dev-genie Architecture Pattern and Integration](../initiative.md)

## Goal

Read the dev-genie orchestration skill and the existing `guardrails` and `audit` registry entries, and produce a short contract-reference document inside the initiative directory. The doc captures the exact registry-row shape (the four required fields), the install-check signal patterns, the setup-command convention, and the post-setup verification convention so subsequent tasks can author a katana entry without re-discovering this.

## Deliverables

- Create: `/Users/danielcassil/Code/katana/.metis/strategies/NULL/initiatives/devgenie-architecture-and-integration/contract-reference.md`

## Architecture & Contracts

The reference doc must include these sections, populated by reading the source files:

1. **Source files read** — list with absolute paths:
   - `/Users/danielcassil/Code/gaurd-rails-boilerplate/dev-genie/skills/orchestration/SKILL.md`
   - `/Users/danielcassil/Code/gaurd-rails-boilerplate/dev-genie/skills/project-detection/SKILL.md`
   - `/Users/danielcassil/Code/gaurd-rails-boilerplate/dev-genie/skills/existing-config-detection/SKILL.md`
   - `/Users/danielcassil/Code/gaurd-rails-boilerplate/dev-genie/skills/reconcile/SKILL.md`

2. **Registry-row shape** — copy the exact bullet structure used by entry "1. guardrails" and "2. audit". Required fields: `Purpose`, `Install check`, `Setup command`, `Post-setup verification`. Optional: `Notes`, `Top-up for already-scaffolded repos`.

3. **Install-check signal patterns** — quote both existing entries verbatim so future authors know the three-signal style: (a) `${CLAUDE_PLUGIN_ROOT}/../<name>/` exists, (b) `<name>/` directory at workspace root, (c) slash command registered.

4. **Setup-command convention** — slash command (e.g. `/scaffold-architecture`, `/audit-init`).

5. **Post-setup verification convention** — concrete file-existence checks against the target repo (e.g. `.audit/audit.config.json`).

6. **Greenfield vs reconciliation paths** — one paragraph each, paraphrased from the orchestration skill.

7. **Final-state checklist** — note that adding a new sub-plugin may require a new line under the orchestration skill's "Final-state checklist" section.

## Do / Don't

- Do quote the orchestration skill verbatim where shape is concerned. Future tasks copy from this doc.
- Don't propose changes to the contract in this task. This is read-only investigation.
- Don't modify any file under `/Users/danielcassil/Code/gaurd-rails-boilerplate/`.
- Don't write katana-specific content yet — this doc is contract-only.
- Don't summarize away the four required fields; list them explicitly.

## Acceptance

- [ ] `contract-reference.md` exists at the path above.
- [ ] All four required registry fields are listed by name.
- [ ] At least one verbatim "Install check" excerpt appears in the doc.
- [ ] The greenfield vs reconciliation distinction is captured in one sentence each.
- [ ] No files outside `.metis/` are modified.

## Dependencies

none