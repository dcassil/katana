---
id: scaffold-guardrails-arch-agent
level: task
title: "Scaffold guardrails:arch-agent-workflow-mcp-workspace skill"
short_code: "KAT-T-0022"
created_at: 2026-05-09T16:30:00+00:00
updated_at: 2026-05-18T21:44:56.887345+00:00
parent: KAT-I-0007
blocked_by: []
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
strategy_id: NULL
initiative_id: devgenie-architecture-and-integrati
---

## Parent Initiative

[KAT-I-0007 dev-genie Architecture Pattern and Integration](../initiative.md)

## Goal

Create a new architecture-pattern skill describing the "agent-workflow plugin with MCP and workspace" shape (the katana shape), using the same SKILL.md frontmatter and section structure as the existing `arch-node-api` skill. The skill is purely descriptive at this stage — it documents the pattern; no boilerplate files are required to ship in this task.

Only execute this task if KAT-T-0021's decision selected Option 2 (new pattern). If Option 1 or 3 was chosen, mark this task `archived: true` without creating files.

## Deliverables

- Create: `/Users/danielcassil/Code/gaurd-rails-boilerplate/guardrails/skills/arch-agent-workflow-mcp-workspace/SKILL.md`
- Modify: `/Users/danielcassil/Code/gaurd-rails-boilerplate/guardrails/skills/guard-rails-catalog/SKILL.md` — append one row to the "Available architectures" table.

## Architecture & Contracts

Frontmatter (copy exactly from `arch-node-api/SKILL.md`, change name+description):

```yaml
---
name: arch-agent-workflow-mcp-workspace
description: Use when scaffolding an agent-workflow plugin that ships a Claude Code plugin surface plus an MCP server plus a doc workspace (e.g. katana, metis). Provides the pattern shape and integration points; lint configs are inherited from the host repo's chosen application architecture.
---
```

Required body sections (in this order):

1. `# Agent-Workflow Plugin with MCP and Workspace — agent-workflow-mcp-workspace`
2. `## What it provides` — bullets: plugin surface (slash commands + skills), MCP server (CRUD/search/transition), doc workspace under `.<name>/`, optional storage backend.
3. `## Components` — three subsections: **Plugin** (Claude Code skills + slash commands), **MCP server** (stdio Node process registered via `claude mcp add` or equivalent), **Workspace** (`.<name>/` directory with `vision.md`, hierarchy dirs, `config.toml`, `<name>.db`).
4. `## Layers / boundaries` — agent-platform adapter → plugin surface → MCP → storage backend → workspace. Each layer is pluggable.
5. `## Key rules` — standalone-first (no dev-genie required), platform-agnostic via adapters, templates+gates author-able in markdown+config.
6. `## Install steps` — describe the two install paths abstractly: (a) standalone via the plugin's own `/<name>-init`; (b) via dev-genie orchestration registry.
7. `## When NOT to use` — pure lint/scaffold needs (use `arch-node-api`), data-access-dominant apps (use `arch-supabase-api`).

Catalog row to append (in `guard-rails-catalog/SKILL.md` table):

```
| `agent-workflow-mcp-workspace` | Agent workflow plugin shipping plugin + MCP + doc workspace (e.g. katana, metis) | `arch-agent-workflow-mcp-workspace` |
```

## Do / Don't

- Don't invent a new skill format. Mirror `arch-node-api/SKILL.md` exactly for frontmatter and section ordering.
- Don't ship `eslint.config.mjs` or `tsconfig.json` under this pattern — it's a meta-pattern; lint comes from the host app's architecture.
- Don't modify other arch-* skills.
- Don't write katana-specific paths into the skill — keep it generic (`<name>`, `.<name>/`).
- Don't add a `Peer deps` or `Copy steps` block — this pattern has no boilerplate to copy.
- Don't touch `dev-genie/` in this task — orchestration wiring is KAT-T-0023.

## Acceptance

- [ ] `arch-agent-workflow-mcp-workspace/SKILL.md` exists with frontmatter matching the shape above.
- [ ] All seven required body sections are present in order.
- [ ] `guard-rails-catalog/SKILL.md` table contains the new row.
- [ ] No `eslint.config.mjs`, `tsconfig.json`, or copy steps appear in the new skill.
- [ ] No other `arch-*` skill is modified.

## Dependencies

KAT-T-0021