---
id: built-in-gate-short-code-format
level: task
title: "Built-in gate: short-code-format"
short_code: "KAT-T-0065"
created_at: 2026-05-09T17:30:00+00:00
updated_at: 2026-05-18T21:11:59.992172+00:00
parent: KAT-I-0002
blocked_by: []
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
strategy_id: NULL
initiative_id: validation-gates
---

## Parent Initiative

[KAT-I-0002](../initiative.md) — Validation Gates and Completeness Checks.

## Goal

Implement the `short-code-format` gate. It validates that `frontmatter.short_code` matches the canonical regex from `/Users/danielcassil/Code/katana/docs/schema/short-codes.md` and that the type-code segment matches the document's `level`.

## Deliverables

- `/Users/danielcassil/Code/katana/src/gates/builtin/short-code-format.ts`

## Architecture & Contracts

Inline-restated `Gate`:

```typescript
import type { Gate, GateContext, GateReason, GateResult, KatanaDocument } from "../types.js";
```

Canonical regex (copy verbatim, escape backslashes for TS):

```typescript
const SHORT_CODE_RE = /^[A-Z][A-Z0-9]{1,9}-(PD|E|US|TH|TL|TU|A)-\d{4}$/;
```

Level → expected type code map:

```typescript
const TYPE_CODE_BY_LEVEL: Record<string, string> = {
  "product-doc": "PD",
  "epic": "E",
  "user-story": "US",
  "task-high-pass": "TH",
  "task-low-pass": "TL",
  "task-ui": "TU",
};
```

Reason codes:

| Code | When |
|------|------|
| `short-code.missing` | `short_code` absent or empty |
| `short-code.invalid-format` | Does not match `SHORT_CODE_RE` |
| `short-code.type-mismatch` | Format valid, but type segment doesn't match level |
| `short-code.parent-format` | `parent` is set but doesn't match `SHORT_CODE_RE` (warning) |
| `short-code.scaffold-format` | `scaffold_task` set but doesn't match `SHORT_CODE_RE` (warning) |

Implementation:

```typescript
export const shortCodeFormatGate: Gate = {
  name: "short-code-format",
  evaluate(doc: KatanaDocument, _ctx: GateContext): GateResult {
    const reasons: GateReason[] = [];
    const fm = doc.frontmatter;
    const code = fm.short_code;
    if (!code || code === "") {
      reasons.push({
        code: "short-code.missing",
        message: "short_code is missing.",
        severity: "error",
        pointer: "frontmatter.short_code",
      });
    } else if (!SHORT_CODE_RE.test(code)) {
      reasons.push({
        code: "short-code.invalid-format",
        message: `short_code "${code}" does not match ${SHORT_CODE_RE.source}.`,
        severity: "error",
        pointer: "frontmatter.short_code",
      });
    } else if (fm.level && TYPE_CODE_BY_LEVEL[fm.level]) {
      const expected = TYPE_CODE_BY_LEVEL[fm.level]!;
      const actual = code.split("-")[1];
      if (actual !== expected) {
        reasons.push({
          code: "short-code.type-mismatch",
          message: `short_code type segment "${actual}" does not match level "${fm.level}" (expected "${expected}").`,
          severity: "error",
          pointer: "frontmatter.short_code",
        });
      }
    }
    if (typeof fm.parent === "string" && fm.parent.length > 0 && !SHORT_CODE_RE.test(fm.parent)) {
      reasons.push({
        code: "short-code.parent-format",
        message: `parent "${fm.parent}" is not a well-formed short code.`,
        severity: "warning",
        pointer: "frontmatter.parent",
      });
    }
    if (typeof fm.scaffold_task === "string" && fm.scaffold_task.length > 0 && !SHORT_CODE_RE.test(fm.scaffold_task)) {
      reasons.push({
        code: "short-code.scaffold-format",
        message: `scaffold_task "${fm.scaffold_task}" is not a well-formed short code.`,
        severity: "warning",
        pointer: "frontmatter.scaffold_task",
      });
    }
    const ok = !reasons.some((r) => r.severity === "error");
    return { gate: "short-code-format", ok, reasons };
  },
};
```

## Do / Don't

- DO use the exact regex source `^[A-Z][A-Z0-9]{1,9}-(PD|E|US|TH|TL|TU|A)-\d{4}$`.
- DO emit warnings (not errors) for malformed `parent` and `scaffold_task` references — those are the parent-readiness gate's job to chase further.
- DON'T check that the referenced parent actually exists in the workspace.
- DON'T support legacy or alternative formats.
- DON'T case-insensitive match — short codes are uppercase only.

## Acceptance

- [ ] File `/Users/danielcassil/Code/katana/src/gates/builtin/short-code-format.ts` exists.
- [ ] Exports `shortCodeFormatGate: Gate` with `name === "short-code-format"`.
- [ ] `KAT-E-9001` on a level=epic doc returns `ok: true`.
- [ ] `KAT-E-42` returns `ok: false` with `short-code.invalid-format`.
- [ ] `KAT-E-0001` on a level=user-story doc returns `ok: false` with `short-code.type-mismatch`.
- [ ] `tsc --strict --noEmit` passes.

## Dependencies

KAT-T-0060 (types).